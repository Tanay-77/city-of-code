import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "Codebase City — Visualize Your GitHub Repository",
  description:
    "Turn any public GitHub repository into an interactive 3D city visualization. Every file becomes a building, every folder becomes a district.",
  keywords: ["github", "visualization", "3d", "codebase", "three.js", "city"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
